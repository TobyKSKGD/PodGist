# FastAPI 后端依赖 (PodGist V2 生产版)
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# AI 模型与语音转录
openai>=1.10.0
openai-whisper>=20231117
modelscope>=1.35.0
pydub>=0.25.0

# 网络与数据处理
requests>=2.31.0
yt-dlp>=2024.1.0

# 其他工具
Pillow>=10.0.0

# modelscope 的额外依赖
addict>=2.4.0
datasets>=2.14.0
oss2>=2.18.0
simplejson>=3.17.0
sortedcontainers>=2.4.0

# 注意：Torch 故意未在此处锁死版本。
# Windows (N卡) 和 Mac (M芯片) 需要分别使用不同的命令安装对应的 Torch 硬件加速版。

# RAG 向量数据库与 Embedding
chromadb>=0.5.0
sentence-transformers>=3.0.0
sse-starlette>=1.8.0