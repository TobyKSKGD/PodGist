# ================= PodGist 完整依赖清单 =================
# 注意：Torch 故意不锁死版本，请根据硬件选择合适的版本安装

# ================= 核心 Web 框架 =================
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# ================= AI 模型与语音转录 =================
openai>=1.10.0
openai-whisper>=20231117
modelscope[audio]>=1.35.0
pydub>=0.25.0

# ================= 网络与数据处理 =================
requests>=2.31.0
yt-dlp>=2024.1.0

# ================= 科学计算基础库（重要！ML 依赖）==================
numpy>=1.26.0
scipy>=1.11.0
scikit-learn>=1.3.0

# ================= RAG 向量数据库与 Embedding =================
chromadb>=0.5.0
sentence-transformers>=3.0.0

# ================= SSE 流式响应 =================
sse-starlette>=1.8.0

# ================= 其他工具 =================
Pillow>=10.0.0

# ================= ModelScope 额外依赖 =================
addict>=2.4.0
datasets>=2.14.0
oss2>=2.18.0
simplejson>=3.17.0
sortedcontainers>=2.4.0
fsspec>=2023.9.0
aiohttp>=3.9.0
# tokenizers 是 sentence-transformers 的核心依赖
tokenizers>=0.15.0

# ================= Windows 音频处理额外依赖 =================
# Windows 上 pydub 需要 soundfile 处理更多格式
soundfile>=0.12.1

# ================= 开发与调试 =================
typer>=0.9.0

# ================= 安装说明 =================
# 请先安装 Torch（根据硬件选择）：
#
#   # macOS Apple Silicon
#   pip install torch torchvision torchaudio
#
#   # Linux / Windows NVIDIA GPU
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
#
#   # 仅 CPU（所有平台）
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# 安装完 Torch 后，一键安装其余所有依赖：
#   pip install -r requirements.txt
