{
  "name": "podgist-desktop",
  "version": "0.2.5",
  "description": "AI-powered timeline player for podcasts and audio content.",
  "author": "TobyKSKGD",
  "license": "MIT",
  "main": "src/main.js",
  "scripts": {
    "start": "electron-builder --dir",
    "prebuild": "node scripts/prebuild.js",
    "check:backend": "python ../scripts/check_backend.py",
    "build:mac": "npm run check:backend && npm run prebuild && electron-builder --config electron-builder.yml --mac",
    "build:win": "npm run check:backend && npm run prebuild && electron-builder --config electron-builder.yml --win",
    "build:all": "npm run prebuild && electron-builder --config electron-builder.yml --win --mac",
    "build:full": "BUILD_TYPE=full npm run build:win",
    "build:lite": "BUILD_TYPE=lite npm run build:win",
    "upload:release": "npm run prebuild && electron-builder --config electron-builder.yml --win --mac --publish always",
    "build:ci:win": "electron-builder --config electron-builder.yml --win",
    "build:ci:mac": "electron-builder --config electron-builder.yml --mac",
    "build:ci:all": "electron-builder --config electron-builder.yml --win --mac"
  },
  "devDependencies": {
    "electron": "^28.3.3",
    "electron-builder": "^24.13.3"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "electron-squirrel-startup": "^1.0.1"
  }
}
